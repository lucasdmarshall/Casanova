# Runtime — pin ranges; install with: pip install -e ".[service,mcp]"
# Or: pip install -r requirements.txt

python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1
httpx>=0.28

# Shared SSRF guard + safe downloader (repo-local package).
# From this directory: pip install -e ../hirara-core
hirara-core>=0.1

# HTTP service
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# MCP front end (Claude Desktop / Cursor / agent runtimes)
mcp>=2.0
