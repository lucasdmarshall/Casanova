# Runtime — the gateway is intentionally light (no tool dependencies).
# pip install -e ".[mcp]"   (or: pip install -r requirements.txt)

httpx>=0.28
fastapi>=0.115
uvicorn[standard]>=0.30

# MCP front end
mcp>=2.0
